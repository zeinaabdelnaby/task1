import 'package:flutter/material.dart';

class image_blood extends StatelessWidget {
  const image_blood({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      "assets/images/blood donat 2.png",
      width: 250,
      height: 200,
    );
  }
}
